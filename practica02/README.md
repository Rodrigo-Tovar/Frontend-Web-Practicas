¿Por qué una unión de valores y no una enumeración?
Type se borra al compilar en lugar de convertirse en un objeto JS

¿Qué se gana con el tipo desconocido en lugar del que acepta todo?
Se puede pasar por el filtro sin fallar al extraer los datos.

¿Por qué la fecha entra como parámetro?
Para usar la misma fecha en todas las funciones sin diferencia en milisegundos
